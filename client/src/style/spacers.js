const base = 1 // rem

const spacer = [
   `${base * 0}rem`, // 0
   `${base * 0.25}rem`, // 1
   `${base * 0.5}rem`, // 2
   `${base * 0.75}rem`, // 3
   `${base * 1}rem`, // 4
   `${base * 1.5}rem`, // 5
   `${base * 2}rem`, // 6
   `${base * 2.5}rem`, // 7
   `${base * 3}rem`, // 8
   `${base * 3.5}rem`, // 9
]

export default spacer
