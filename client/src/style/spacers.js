const base = 1 // rem

const spacer = [
   `${base * 0}rem`,       // 0
   `${base * 0.1875}rem`,  // 1
   `${base * 0.375}rem`,   // 2
   `${base * 0.5625}rem`,  // 3
   `${base * 0.75}rem`,    // 4
   `${base * 1}rem`,       // 5
   `${base * 1.375}rem`,   // 6
   `${base * 1.875}rem`,   // 7
   `${base * 2.5}rem`,     // 8
   `${base * 3.375}rem`,   // 9
]

export default spacer
